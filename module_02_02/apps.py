from django.apps import AppConfig


class Module0202Config(AppConfig):
    """Django app config for Season 2, Lesson 02 — Chunking, Embeddings, Hybrid RAG."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "module_02_02"
    verbose_name = "S2·L02 — Chunking, Embeddings & Hybrid RAG"
